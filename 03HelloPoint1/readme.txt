写了60行代码，错了6处，要好好反省一下了，中文标点符号，大小写，字母拼错，</title>没有加/。这都是粗细大意啊
 还是之前的顺序
 获取id getElementById()->
 获取绘图上下文getWebGLContext()->
 初始化着色器initshaders()->
 设置颜色ClearColor()->
 清空颜色Clear()->
 画图drawArrays()